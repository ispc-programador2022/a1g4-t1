from itertools import combinations
import random
num_rand = []

def genrnd(a, b):
    for i in range(50):
        num_rand.append(random.uniform(a,b))
        
def comb_suma(c):
    list = combinations(c, 2)
    for i in list(list):
        print(sum(i))
        
genrnd(1000, 2000)
print(num_rand)
