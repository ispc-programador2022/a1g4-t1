import random
import statistics

num_rand = []
maximo_num_rand = []

def genrnd(a, b):
    for i in range(500000000000000000):
        num_rand.append(random.uniform(a,b))
      
def maximo(h):
    maximo_num_rand = max(h)
    return maximo_num_rand

genrnd(100, 200)

print(num_rand)


print(maximo(num_rand))
